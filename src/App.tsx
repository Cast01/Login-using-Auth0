import { RoutesFunc } from "./Routes";
import { BrowserRouter } from "react-router-dom";

function App() {
  return (
    <BrowserRouter>
      <RoutesFunc />
    </BrowserRouter>
  );
}

export default App;
